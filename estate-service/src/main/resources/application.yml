spring:
  application:
    name: estate-service
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/estate-service
    username: root
    password: root
  jpa:
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/fortunatishomes
server:
  port: 8182

service:
  param:
    keycloak-service:
      token-url: http://localhost:8080/realms/fortunatishomes/protocol/openid-connect/token
      client-id: static-service
      client-secret: XuCIzeFc43F3NqBYqpaAYoab7NBD08et
    static-service:
      base-url: http://localhost:8181
      public-prefix: /public
      endpoint-prefix: /api/v1/static-api
    file-storage:
      dir: https://fotunatishome.s3.amazonaws.com/
      users-dir: fortunatis-real-estate/uploads/users/
      admin-dir: fortunatis-real-estate/uploads/admin/
cloud:
  aws:
    s3:
      bucket: fotunatishome
    region:
      static: us-east-1
    stack:
      auto: false
    credentials:
      accessKey: AKIA6BSS4IPSO3FL5LDX
      secretKey: 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

springfox:
  documentation:
    type: swagger_2
    enabled: true  # Set false to disable
  swagger-ui:
    base-url: /swagger-ui/  # URL to access swagger UI


logging:
  level:
    root: INFO
springdoc:
  show-actuator: false
  enable-kotlin: false
  swagger-ui:
    doc-expansion: none
