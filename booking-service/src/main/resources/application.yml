spring:
  application:
    name: booking-service
  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health,info
